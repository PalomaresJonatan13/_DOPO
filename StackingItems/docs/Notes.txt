boolean attributes 'removable' (hierarchical, fearful), 'cupDependent' (fearful, maybe not necessary since it would be in .onPush), 'inverted' (crazy)


Cup, Lid -> TowerItem (abstract)
OpenerCup, HierarchicalCup -> Cup
FearfulLid, CrazyLid -> Lid


OpenerCup (push): calls stackingItems, and calls .pop over all lids at the top (maybe a try/catch, because of the fearful lid), and then in Tower getCup and add it to towerItems
HierarchicalCup (push): stackingItems, getCup, swaps with smaller items (loop).
Fearful (push): check if the cup is in activeItems
Crazy (cover): easy ...


Create an exception class, or several of them


use .onPush, .onPop, .onRemove, .onCover, instead of .push, .pop, .remove, .cover