other Cup: when it is removed, every item between it and the next item with greater index is removed too.

Organize the code in Tower

create an isEmpty method.

isAssociatedItem(item) ????

change the name of 'updateLiddedState'

complete the TowerVisibleTest according to the changes made

for the visible trick, make the items invisible

visibility trick in new items' .onPush